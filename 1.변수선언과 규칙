#include<iostream> //전처리 지시지
/*
c++에서 함수를 사용하고자 한다면 반드시 그 함수의 원형을 미리 정의해야댐
;는 종결자의 역할을 함*/
using namespace std; //이건 코드 앞에 std:: 를 일일히 붙이는것을 안해도 되게 만들어줌 ㅋㅋ 개이득
int main(){ /* {
	//c++코드에는 항상 main 의 이름을 가지고 있는 함수를 가지고 있어야 한다.
	cout << "hello world" << endl; //endl 은 문장의 줄을 바꾸는 역할
	cout << "hello"<<endl;
	cout << "world";
	return 0;
}*/

	//1.변수란 변할 수 있는 수
	//cf.상수: 변할 수 없는 수
	/*
	1.변수의 자료형
	2.변수의 이름
	3.변수가 어디에 저장되는가(메모리영역)?
	*/
	
	/*변수의 이름을 정하는 규칙
	1. 숫자로 시작할 수 없음
	2.c++에서 사용하고 있는 키워드는 사용할 수 없음
	3.white space를 사용할 수 없음
	int 777aaa;
	int return;
	int aaa aaa ->이런식으로 띄어쓰는거안됨 */
	int a; //이런걸 선언이라고 함
	a = 7; // 이렇게 선언된 문자에 값을 넣는것을 대입이라고 함
	
	int b = 3; //선언과 대입 이외에도, 선언과 동시에 값을 넣는 것을 초기화라고함 int b를 선언하고 바로 3을 넣는거
	//변수는 사용되기 이전에 정의되어야 한다.
	cout << a <<" " << b << endl;
	return 0;
}
